

// c) Si el número es positivo, sumar al total de positivos, sino al total de negativos.
if (numero > 0) {
    totalPositivos += numero;
} else {
    totalNegativos += numero;
}

// d) Si x es mayor que y, y z es menor que 20, leer un valor para p.
if (x > y && z < 20) {
    System.out.print("Ingrese el valor para p: ");
    p = sc.nextDouble();
}

// e) Si distancia es mayor que 20 y menor que 35, leer un valor para tiempo.
if (distancia > 20 && distancia < 35) {
    System.out.print("Ingrese el valor para tiempo: ");
    tiempo = sc.nextDouble();
}

